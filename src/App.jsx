import TestSeriesTracker from './TestSeriesTracker'; 
// Make sure TestSeriesTracker.jsx is in the same 'src' directory

function App() {
  // We are rendering only our custom component, which contains everything.
  return (
    <TestSeriesTracker />
  );
}

export default App;